plugins {
    id("ireader-plugin")
}

pluginConfig {
    id.set("io.github.ireaderorg.plugins.coral-reef")
    name.set("Coral Reef")
    version.set("1.0.0")
    versionCode.set(1)
    description.set("Tropical ocean colors with warm coral and cool cyan accents")
    author.set("IReader Team")
    authorWebsite.set("https://github.com/IReaderorg")
    type.set(PluginType.THEME)
    permissions.set(emptyList())
}
